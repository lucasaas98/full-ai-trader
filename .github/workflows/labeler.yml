name: "Pull Request Labeler"

on:
    pull_request:
        branches: [main]

jobs:
    triage:
        permissions:
            contents: read
            pull-requests: write
        runs-on: self-hosted
        steps:
            - uses: actions/labeler@v4
